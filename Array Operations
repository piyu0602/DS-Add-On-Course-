ARRAY OPERATIONS
#include<iostream>
using namespace std;
int main()
{
    int arr1[] = {1,2,3,4,5,11,25,25,45,66};
    int arr2[3];
    int arr3[1]={2};
    int temp[10];
    int sum=0;
    int flag=0;
    int search;
    int j=0;
    int len = sizeof(arr1)/sizeof(arr1[0]);
//     for(int i=0;i<5;i++)
//     {
//         cout<<arr1[i]<<endl;
//     }
//     cout<<"Length is:"<<len<<endl;
//     cout<<"Reverse array is:";
//   for(int i=len-1;i>=0;i--)
//   {
       
//       cout<<arr1[i]<<endl;
//   }
   
//   for(int j =0;j<len;j++)
//   {
//       sum+=arr1[j];
//   }
//   cout<<"Sum is :";
//   cout<<sum;

    // cout<<"Enter the number to be searched: ";
    // cin>>search;
    
    // for(int i = 0;i<len;i++)
    // {
    //     if(search==arr1[i])
    //     {
    //         flag=1;
    //     }
        
    // }
    // if(flag==1)
    // {
    //     cout<<"Found!!";
    // }
    for(int i=0;i<len;i++)
    {
        if(arr1[i]>arr1[i+1])
        {
            temp[i] = arr[i];
            arr[i]
        }
    }
    for(int i=0;i<len;i++)
    {
        if(arr1[i]!=arr1[i+1])
        {
            temp[j++]=arr1[i]; // post incremented 
        }
    }
    // temp[j++] = arr1[len-1];
    //  int lent = sizeof(temp)/sizeof(temp[0]);
    // cout<<lent;
    for(int i=0;i<len;i++)
    {
        cout<<temp[i]<<endl;
    }
    return 0;
    
}

